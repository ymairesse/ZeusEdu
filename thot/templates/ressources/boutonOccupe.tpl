<button type="button"
    class="btn btn-danger btn-xs btn-block btn-reserved"
    data-acronyme="{$acronyme}"
    data-heure="{$heure}"
    data-date="{$date}"
    data-idressource="{$idRessource}"
    title="{$nom}">
    <span class="micro">{$acronyme}</span>
</button>
