<button type="button"
    class="btn btn-success btn-xs btn-block btn-reservation"
    data-heure="{$heure}"
    data-date="{$date}"
    data-idressource="{$idRessource}"
    title="">
        <i class="fa fa-user"></i>
</button>
